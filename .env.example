# Copy this file to .env and replace the sample values.
# Then load it before running Ansible:
#   set -a
#   . ./.env
#   set +a
#   ansible-playbook playbooks/provision_hetzner.yml

HETZNER_API_TOKEN=your_hetzner_api_token_here
SERVER_NAME=ai-server
SERVER_TYPE=cx22
IMAGE=ubuntu-24.04
LOCATION=nbg1
SSH_KEY_NAME=default
SSH_KEY_NAMES=default,team-laptop
